name: CI

on:
  push:
    branches:
      - '*'
    paths-ignore:
      - '**.md' 
  pull_request:
    branches:
      - '*'

jobs:
  ubuntu2004:  
    runs-on: ubuntu-latest
    container:
    # This is the cached docker image dated 17/06/2021
      image: "ubuntu:20.04"
    steps:
    - name: Dependencies
      run: |
        # Install extra ubuntu packages
        apt-get update
        apt-get install -y sudo        
        sudo apt-get update
        # Install dotnet-sdk packages
        sudo apt-get install -y wget
        wget https://packages.microsoft.com/config/ubuntu/20.04/packages-microsoft-prod.deb -O packages-microsoft-prod.deb
        sudo dpkg -i packages-microsoft-prod.deb
        sudo apt-get install -y apt-transport-https
        sudo apt-get update
        sudo apt-get install -y dotnet-sdk-3.1
        # Install mono and msbuild from stable repo
        sudo apt-get install -y gnupg ca-certificates
        sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys 3FA7E0328081BFF6A14DA29AA6A19B38D3D831EF
        echo "deb https://download.mono-project.com/repo/ubuntu stable-focal main" | sudo tee /etc/apt/sources.list.d/mono-official-stable.list
        sudo apt-get update
        # Install extra packages required for dotdevelop build
        sudo apt-get install -y sed build-essential intltool fsharp gtk-sharp2
        sudo apt-get install -y software-properties-common
        sudo apt-get update
      shell: bash
# Don't checkout repo yet - check packages above first
#    - uses: actions/checkout@v2
#      with:
#        submodules: true
    - name: Verify OS and toolchain versions
      run: |
        uname -a
        make --version
        mono -V
        msbuild -version
        dotnet --info
      shell: bash
    - name: Configure and build
      run: |
# Don't take build any further yet        
#        ./configure --profile=gnome
#        make
      shell: bash
